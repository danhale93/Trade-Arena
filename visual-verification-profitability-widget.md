# Profitability Widget Visual Verification

- Desktop full-page preview at 1280x720: the new Simulated Route Profitability panel renders between configuration and execution history, matches the cyberpunk palette, shows four summary cards, and presents a clear empty state when no simulation records exist.
- Mobile full-page preview at 375x812: the panel stacks cleanly below the multi-chain configuration, summary cards remain readable in a two-column grid, and the empty state stays within the viewport width without horizontal overflow.
- The chart is intentionally empty until a real simulation check persists a route record; no fabricated profitability points are rendered.
